version: '3'
services:
  tautulli:
    image: ghcr.io/tautulli/tautulli
    container_name: tautulli
    restart: unless-stopped
    volumes:
      - /srv/tautulli/:/config
    environment:
      - PUID=0
      - PGID=0
      - TZ=Europe/Zurich
    ports:
      - 8186:8181
